language: python
python:
  - "3.8"
install:
  - make install
  - pip install -r requirements_test.txt

before_script:
  - make verify-lint
script:
  - make test

deploy:
  provider: pypi
  username: "__token__"
  password: "teste"
  on:
    all_branches: true
    python: 3.8
